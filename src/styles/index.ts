import { createStitches } from '@stitches/react'

export const {
  config,
  theme,
  styled,
  globalCss,
  keyframes,
  getCssText,
  createTheme,
  css,
} = createStitches({
  theme: {
    colors: {
      background: '#8257e6',
    },
  },
})
